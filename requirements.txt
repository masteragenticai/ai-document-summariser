# AI Solutions Course - Month 1 Document Summariser
# Additional dependencies not installed by setup.sh

# Data Processing (these get installed automatically with other packages,
# but we list them to ensure specific versions)
pydantic==2.11.7


# Basic utilities
pandas==2.3.1
PyYAML==6.0.2
